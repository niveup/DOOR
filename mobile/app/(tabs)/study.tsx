import { useMemo, useRef, useState } from "react";
import { ScrollView, StyleSheet, Text, View } from "react-native";
import { BottomSheetModal, BottomSheetScrollView, BottomSheetTextInput } from "@gorhom/bottom-sheet";
import { Ionicons } from "@expo/vector-icons";
import * as Haptics from "expo-haptics";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { AppScreen } from "@/src/components/screen";
import { ActionButton, Card, Chip, EmptyState, IconButton, LoadingCard, Metric, ProgressBar, SectionTitle, ui } from "@/src/components/ui";
import { api } from "@/src/services/api";
import { todayInKolkata } from "@/src/lib/format";
import { TrackerSubject } from "@/src/types/domain";
import { colors } from "@/src/theme/tokens";

type SheetMode = "log" | "goal";

export default function StudyScreen() {
  const client = useQueryClient();
  const sheetRef = useRef<BottomSheetModal>(null);
  const [sheetMode, setSheetMode] = useState<SheetMode>("log");
  const [selectedSubject, setSelectedSubject] = useState<TrackerSubject | null>(null);
  const [customSubject, setCustomSubject] = useState("");
  const [hours, setHours] = useState("1");
  const [questions, setQuestions] = useState("0");
  const [notes, setNotes] = useState("");
  const [goal, setGoal] = useState("4");
  const tracker = useQuery({ queryKey: ["tracker"], queryFn: api.tracker.status });
  const open = (mode: SheetMode) => { setSheetMode(mode); if (mode === "goal") setGoal(String(tracker.data?.dailyAvailableHours || 4)); else { setSelectedSubject(tracker.data?.subjects[0] || null); setCustomSubject(""); setHours("1"); setQuestions("0"); setNotes(""); } sheetRef.current?.present(); };
  const logMutation = useMutation({ mutationFn: api.tracker.log, onSuccess: async () => { await Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success); sheetRef.current?.dismiss(); client.invalidateQueries({ queryKey: ["tracker"] }); } });
  const goalMutation = useMutation({ mutationFn: api.tracker.goal, onSuccess: () => { sheetRef.current?.dismiss(); client.invalidateQueries({ queryKey: ["tracker"] }); } });
  const saveLog = () => {
    const finalSubjectName = selectedSubject?.subjectName || customSubject.trim() || "General Study";
    const finalSubjectId = selectedSubject?.subjectId || 1;
    logMutation.mutate({ logDate: todayInKolkata(), timeBlock: "Mobile log", subjectId: finalSubjectId, subjectName: finalSubjectName, hoursStudied: Math.max(0, Number(hours) || 0), questionsSolved: Math.max(0, Number(questions) || 0), notes: notes.trim() });
  };
  const readiness = tracker.data?.overallReadiness || 0;
  const totalHours = tracker.data?.subjects.reduce((sum, subject) => sum + subject.cumulativeHours, 0) || 0;
  const totalQuestions = tracker.data?.subjects.reduce((sum, subject) => sum + subject.cumulativeQuestions, 0) || 0;
  const warnings = tracker.data?.subjects.filter((subject) => subject.isNeglected || subject.hasAvoidanceWarning) || [];
  return <AppScreen title="GATE Progress" subtitle="Your honest signal—not just a streak." refreshing={tracker.isRefetching} onRefresh={tracker.refetch} action={<IconButton icon="add" label="Log study session" onPress={() => open("log")} tone={colors.cyan} />}>
    {tracker.isLoading ? <LoadingCard label="Loading your GATE signal…" /> : null}
    {tracker.error ? <EmptyState icon="cloud-offline-outline" title="Tracker is offline" description="Your cached progress will return when the server reconnects." action={<ActionButton label="Retry" compact onPress={() => tracker.refetch()} />} /> : null}
    {tracker.data ? <>
      <Card style={styles.readiness}><View style={ui.spread}><View><Text style={styles.overline}>OVERALL READINESS</Text><Text style={styles.readinessValue}>{readiness}%</Text></View><View style={styles.goalBox}><Text style={styles.goalLabel}>DAILY GOAL</Text><Text style={styles.goalValue}>{tracker.data.dailyAvailableHours}h</Text><ActionButton label="Edit" compact tone="ghost" onPress={() => open("goal")} /></View></View><ProgressBar value={readiness} tone={readiness >= 65 ? colors.emerald : colors.cyan} /></Card>
      <Card style={styles.metrics}><Metric label="Cumulative hours" value={`${totalHours.toFixed(1)}h`} accent={colors.cyan} /><Metric label="Questions solved" value={totalQuestions} accent={colors.emerald} /><Metric label="At risk" value={warnings.length} accent={warnings.length ? colors.rose : colors.amber} /></Card>
      {warnings.length ? <Card style={styles.warning}><View style={styles.warningHeader}><Ionicons name="warning-outline" size={19} color={colors.amber} /><Text style={styles.warningTitle}>Avoidance radar</Text></View><Text style={styles.warningText}>{warnings.slice(0, 3).map((subject) => subject.subjectName).join(", ")} {warnings.length > 3 ? `and ${warnings.length - 3} more` : ""} need an honest next session.</Text></Card> : null}
      <SectionTitle title="Subject readiness" />
      <View style={styles.subjectList}>{tracker.data.subjects.map((subject) => <SubjectRow key={subject.subjectId} subject={subject} />)}</View>
      <SectionTitle title="Recent study logs" trailing={<ActionButton label="Log session" compact onPress={() => open("log")} />} />
      {tracker.data.logs.length ? <View style={styles.logList}>{tracker.data.logs.slice(0, 5).map((log) => <Card key={log.id} style={styles.logRow}><View style={styles.logIcon}><Ionicons name="time-outline" color={colors.cyan} size={17} /></View><View style={styles.logCopy}><Text style={styles.logTitle}>{log.subjectName}</Text><Text style={styles.logMeta}>{log.logDate} · {log.questionsSolved} questions {log.notes ? `· ${log.notes}` : ""}</Text></View><Text style={styles.logHours}>{log.hoursStudied}h</Text></Card>)}</View> : <Card><Text style={styles.muted}>Log your first focused block—the tracker will handle the aggregation.</Text></Card>}
      {tracker.data.weeklyAnalysis ? <Card><Text style={styles.analysisLabel}>WEEKLY JUJUM READ</Text><Text style={styles.analysis}>{tracker.data.weeklyAnalysis}</Text></Card> : null}
    </> : null}
    <BottomSheetModal ref={sheetRef} snapPoints={["72%"]} backgroundStyle={styles.sheet} handleIndicatorStyle={styles.handle}><BottomSheetScrollView contentContainerStyle={styles.sheetContent}>{sheetMode === "log" ? <LogForm subjects={tracker.data?.subjects || []} selected={selectedSubject} customSubject={customSubject} onCustomSubject={setCustomSubject} onSelect={setSelectedSubject} hours={hours} questions={questions} notes={notes} onHours={setHours} onQuestions={setQuestions} onNotes={setNotes} busy={logMutation.isPending} onSave={saveLog} /> : <GoalForm value={goal} onChange={setGoal} busy={goalMutation.isPending} onSave={() => goalMutation.mutate(Number(goal))} />}</BottomSheetScrollView></BottomSheetModal>
  </AppScreen>;
}

function SubjectRow({ subject }: { subject: TrackerSubject }) { const rating = subject.latestRating || 0; const tone = subject.hasAvoidanceWarning || subject.isNeglected ? colors.rose : rating >= 4 ? colors.emerald : rating >= 3 ? colors.amber : colors.cyan; return <Card style={styles.subjectRow}><View style={styles.subjectTop}><View style={styles.subjectCopy}><Text style={styles.subjectTitle}>{subject.subjectName}</Text><Text style={styles.subjectMeta}>{subject.cumulativeHours.toFixed(1)}h total · {subject.cumulativeQuestions} questions</Text></View><View style={[styles.rating, { borderColor: tone }]}><Text style={[styles.ratingText, { color: tone }]}>{rating || "—"}/5</Text></View></View><ProgressBar value={rating * 20} tone={tone} />{subject.isNeglected || subject.hasAvoidanceWarning ? <Text style={[styles.flag, { color: tone }]}>{subject.hasAvoidanceWarning ? "Repeated avoidance pattern" : "No recent progress logged"}</Text> : null}</Card>; }
function LogForm({ subjects, selected, customSubject, onCustomSubject, onSelect, hours, questions, notes, onHours, onQuestions, onNotes, busy, onSave }: { subjects: TrackerSubject[]; selected: TrackerSubject | null; customSubject: string; onCustomSubject: (v: string) => void; onSelect: (subject: TrackerSubject) => void; hours: string; questions: string; notes: string; onHours: (value: string) => void; onQuestions: (value: string) => void; onNotes: (value: string) => void; busy: boolean; onSave: () => void }) { return <View style={styles.form}><Text style={styles.sheetTitle}>Log study block</Text><Text style={styles.sheetSubtitle}>The dashboard updates optimistically, then securely syncs in the background.</Text><Text style={styles.fieldLabel}>SUBJECT</Text>{subjects.length > 0 ? <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.picker}>{subjects.map((subject) => <Chip key={subject.subjectId} label={subject.subjectName} active={selected?.subjectId === subject.subjectId} tone={colors.cyan} onPress={() => onSelect(subject)} />)}</ScrollView> : <BottomSheetTextInput style={styles.input} value={customSubject} onChangeText={onCustomSubject} placeholder="e.g. Operating Systems" placeholderTextColor={colors.textFaint} />}<View style={styles.fields}><View style={styles.smallField}><Text style={styles.fieldLabel}>HOURS</Text><BottomSheetTextInput style={styles.input} value={hours} onChangeText={onHours} keyboardType="decimal-pad" placeholder="1" placeholderTextColor={colors.textFaint} /></View><View style={styles.smallField}><Text style={styles.fieldLabel}>QUESTIONS</Text><BottomSheetTextInput style={styles.input} value={questions} onChangeText={onQuestions} keyboardType="number-pad" placeholder="0" placeholderTextColor={colors.textFaint} /></View></View><Text style={styles.fieldLabel}>NOTE (OPTIONAL)</Text><BottomSheetTextInput style={[styles.input, styles.multiline]} value={notes} onChangeText={onNotes} multiline placeholder="What did you learn or get stuck on?" placeholderTextColor={colors.textFaint} /><ActionButton label={busy ? "Logging…" : "Save study log"} icon="checkmark" disabled={busy} onPress={onSave} /></View>; }
function GoalForm({ value, onChange, busy, onSave }: { value: string; onChange: (value: string) => void; busy: boolean; onSave: () => void }) { return <View style={styles.form}><Text style={styles.sheetTitle}>Set daily availability</Text><Text style={styles.sheetSubtitle}>This is capacity, not pressure. Pick the time you can protect most days.</Text><BottomSheetTextInput style={styles.goalInput} value={value} onChangeText={onChange} keyboardType="decimal-pad" placeholder="4" placeholderTextColor={colors.textFaint} /><Text style={styles.goalHint}>hours per day</Text><ActionButton label={busy ? "Saving…" : "Save daily goal"} icon="checkmark" disabled={busy} onPress={onSave} /></View>; }
const styles = StyleSheet.create({ readiness: { backgroundColor: "#0c2330", borderColor: "#155e75", gap: 13 }, overline: { color: colors.cyan, fontSize: 10, letterSpacing: 1, fontWeight: "900" }, readinessValue: { color: colors.text, fontSize: 39, fontWeight: "900", fontVariant: ["tabular-nums"] }, goalBox: { alignItems: "flex-end", gap: 5 }, goalLabel: { color: colors.textFaint, fontSize: 9, letterSpacing: 0.8, fontWeight: "900" }, goalValue: { color: colors.amber, fontSize: 19, fontWeight: "900" }, metrics: { flexDirection: "row", gap: 8 }, warning: { backgroundColor: "#2e1f06", borderColor: "#78350f" }, warningHeader: { flexDirection: "row", alignItems: "center", gap: 7 }, warningTitle: { color: colors.amber, fontSize: 14, fontWeight: "900" }, warningText: { color: colors.textMuted, fontSize: 13, lineHeight: 19 }, subjectList: { gap: 8 }, subjectRow: { gap: 10 }, subjectTop: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", gap: 12 }, subjectCopy: { flex: 1, gap: 3 }, subjectTitle: { color: colors.text, fontSize: 14, fontWeight: "800" }, subjectMeta: { color: colors.textFaint, fontSize: 11 }, rating: { borderWidth: 1, borderRadius: 10, paddingHorizontal: 8, paddingVertical: 5 }, ratingText: { fontSize: 11, fontWeight: "900" }, flag: { fontSize: 11, fontWeight: "700" }, logList: { gap: 8 }, logRow: { flexDirection: "row", alignItems: "center", padding: 11, gap: 9 }, logIcon: { width: 32, height: 32, alignItems: "center", justifyContent: "center", borderRadius: 10, backgroundColor: "#083344" }, logCopy: { flex: 1, gap: 3 }, logTitle: { color: colors.text, fontSize: 13, fontWeight: "800" }, logMeta: { color: colors.textFaint, fontSize: 10, lineHeight: 14 }, logHours: { color: colors.cyan, fontWeight: "900", fontSize: 13 }, analysisLabel: { color: colors.violet, fontSize: 10, fontWeight: "900", letterSpacing: 1 }, analysis: { color: colors.textMuted, fontSize: 13, lineHeight: 20 }, muted: { color: colors.textMuted, fontSize: 13 }, sheet: { backgroundColor: colors.surface }, handle: { backgroundColor: colors.border }, sheetContent: { padding: 20, paddingBottom: 48 }, form: { gap: 13 }, sheetTitle: { color: colors.text, fontSize: 23, fontWeight: "900" }, sheetSubtitle: { color: colors.textMuted, fontSize: 13, lineHeight: 19, marginBottom: 5 }, fieldLabel: { color: colors.textMuted, fontSize: 10, letterSpacing: 0.8, fontWeight: "900" }, picker: { gap: 8 }, fields: { flexDirection: "row", gap: 10 }, smallField: { flex: 1, gap: 7 }, input: { borderWidth: 1, borderColor: colors.border, color: colors.text, borderRadius: 12, minHeight: 46, paddingHorizontal: 13, fontSize: 14 }, multiline: { minHeight: 90, textAlignVertical: "top", paddingTop: 12 }, goalInput: { borderWidth: 1, borderColor: colors.cyan, color: colors.text, backgroundColor: "#083344", borderRadius: 14, textAlign: "center", fontSize: 40, fontWeight: "900", paddingVertical: 12 }, goalHint: { color: colors.textMuted, textAlign: "center", fontSize: 13, marginTop: -7 } });
